"""
Jobs endpoints removed.

The Jobs feature has been fully removed from this repository per project request.
This file is intentionally left as a placeholder to avoid import errors during a transition.
If you want to restore the Jobs feature, reintroduce the original router implementation.
"""

from fastapi import APIRouter

router = APIRouter()
