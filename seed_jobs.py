"""
Jobs seeding helper removed.

This script previously invoked the backend seed endpoint to create example jobs.
Since the Jobs feature is removed, this script is now a placeholder.
"""

def _placeholder():
    raise RuntimeError("Jobs seeding script removed; Jobs feature is no longer available.")
